import React from "react";

const ServiceText = (props) => {
  return (
   <p class="font-bold text-1xl text-gray-400 text-justify">
		{props.text}
	</p>
  );
};

export default ServiceText;
